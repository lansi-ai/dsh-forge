# 02 · 需求与产品定位

## 1. 产品定位

**dsh-forge**：面向个人的 DeepSeek Harness 桌面客户端——本质是「把桌面操作系统变成了 Harness 的可插拔能力层」。

- **不是**：网页套壳（不依赖外置浏览器与外部 HTTP 端口，宿主内嵌、零端口）。
- **是**：与官方同内核（同一 Cordis Host 插件树、同一会话/轨迹/沙箱语义）的桌面应用，
  其桌面特有能力全部通过官方插件机制注入；主面自 **M6 起为全量自绘**（2026-08-27 D-20 启用 · [ADR-006](adr/adr-006-custom-ui.md)，进度见 [`plugin-inventory.md`](plugin-inventory.md)），
  **官方 Web UI 发行物**为 M1–M3 底座与兼容窗口（`dsh-ui://` 内嵌，见 [`13-ui-design.md`](13-ui-design.md)）。

### 目标用户
1. 已将 DSH 作为日常 AI 编程/自动化工具、希望获得原生桌面体验（托盘驻留、快捷键唤出、多窗口）的开发者；
2. **现有 Web UI 的用户**：对浏览器式布局（三栏+tab、深层管理页）不满，期待桌面工作流（键盘优先、命令面板、信息密度）的开发者；
3. 不愿/不便配置 Node 与插件环境的普通用户（一包安装即用）；
4. 需要「后台持续运行 + 会话并行 + 系统级集成」（通知、剪贴板、文件关联）的重度用户。

### 与官方 Web 的关系
- 数据完全兼容：同一 `$DSH_HOME`、同一 profile、同一 session 持久化（JSONL）——Web 与桌面可并存（默认不同时开端口）。
- **主面**（M1–M3 底座）= 官方 Web UI 发行物以桌面内嵌形态运行；**2026-08-27 D-20 起主面切换为全量自绘**（[ADR-006](adr/adr-006-custom-ui.md) 已启用，逐槽位替换官方 ui-*，进度见 [`plugin-inventory.md`](plugin-inventory.md)）；旧 client 插件经零端口 bundle 兼容面（ADR-007）继续可用。

## 2. 需求矩阵

优先级：**P0** = M1–M3 完成（可天天用）；**P1** = M4–M5；**P2** = M6 及以后。

### 2.1 核心（P0）

| ID | 需求 | 说明 | 判定标准 |
| --- | --- | --- | --- |
| R-01 | 一包安装即用 | 安装后无需 Node/pnpm/额外配置 | 全新 Win10/11 机器安装后直接对话 |
| R-02 | 官方 UI 复用主面 | 主面直接内嵌官方 Web UI 发行物（`dsh-ui://` 加载），不做像素级自绘；键盘优先/状态投影等体验增强通过官方槽位注入 | M1 起可打开官方 UI 并完成日常对话全流程 |
| R-02b | 旧插件兼容 | 用户现有 web client 插件（terminal/rule-manager/restart）不被丢弃 | host 半零改动可用；client 半随官方 UI 主面原样可用 |
| R-03 | 宿主内嵌、零端口 | 默认不监听任何 HTTP 端口 | `netstat` 无 308x 监听；卸载后无残留 |
| R-04 | 同一数据目录 | 与 `dsh web` 共享 profile / sessions / credentials | 切回 Web 会话历史无缝 |
| R-05 | 会话持久与后台 | 关闭窗口不杀宿主（托盘驻留）；任务在后台继续 | 关窗→托盘→agent 继续跑→通知完成 |
| R-06 | 桌面原生通知 | agent 完成/需要审批/出错 → 系统通知（可点开定位会话） | 通知点击聚焦对应会话窗口 |
| R-07 | 全局热键唤醒 | 默认 `Ctrl+Shift+Space` 呼出快速输入（Spotlight 式），可配置 | 任意应用内热键唤起即打字即回车 |
| R-08 | 托盘菜单 | 会话列表、状态、快速问答、退出 | 托盘即用 |

### 2.2 深度集成（P0/P1）

| ID | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| R-09 | 多窗口/多会话 | 一个会话可拖出独立窗口；多会话并行各自窗口 | P1 |
| R-10 | 桌面面板 | 通过官方槽位注入「桌面」面板（状态、托盘控制、快捷键管理、更新） | P0 |
| R-11 | 剪贴板桥 | 允许 agent 经审批写剪贴板（复制代码片段、文件路径） | P1 |
| R-12 | 开机自启 | 可选驻留系统托盘后台 | P1 |
| R-13 | `dsh://` 协议唤起 | 浏览器/终端 `dsh://open?session=xxx` 唤起桌面并定位 | P1 |
| R-14 | 文件关联/拖放 | 把文件/文件夹拖进输入框 → 作为附件/工作区路径 | P2 |
| R-15 | 桌面会话审计 | 桌面动作（托盘点击、通知、热键、协议唤起）进会话轨迹/日志 | P1 |
| R-16 | 命令面板 | `Ctrl+K` 面板：会话切换、命令、设置、插件开关（自绘交互，非浏览器式） | P1 |
| R-17 | 设置窗口 | 原生设置窗口（通用/模型/插件/桌面能力）；技能/插件管理等重面 M2 先由官方 UI 管理页承载 | P0 |
| R-18 | 自动更新 | 桌面与应用数据分离；新版本下载→校验→安装→重启 | P1 |
| R-19 | 离线启动 | 无网络时 UI 与本地历史可用；模型调用给出可恢复报错 | P1 |
| R-20 | 崩溃自愈 | 宿主异常退出后自动重启并恢复会话视图（事件溯源重建） | P2 |

### 2.3 可选增强（P2）

| ID | 需求 | 说明 |
| --- | --- | --- |
| R-21 | 主题化 | 基于**自有 token** 的桌面主题包（light/dark/自定义；与官方 skin 概念对齐但走我们的 token 面） |
| R-22 | 本地用量看板 | 基于 session-telemetry-otel 本地物化：token/时间/工具分布图表 |
| R-23 | Git 集成面 | 工作区状态、diff 预览（与 task 看板类功能，参考社区 2.6.0） |
| R-24 | 语音/朗读 | 系统 TTS 朗读 assistant 消息（无障碍向） |

### 2.4 UI / UX 差异化要求（M6 主线 · D-20 已启用；原按 P2 记账，详见 [`13-ui-design.md`](13-ui-design.md)）

> **状态**：本节与 13-ui-design.md / ADR-006 已随 **2026-08-27 D-20 启用**（全量自绘 M6 主线）；原「二期可选 / P2 记账」为 L2 底座阶段的优先级记录，实际逐槽位替换进度见 [`plugin-inventory.md`](plugin-inventory.md)。

| ID | 需求 | 说明 | 优先级 |
| --- | --- | --- | --- |
| U-01 | 键盘优先 | 任意操作 ≤2 键可达；方向键/j-k 切换会话与消息 | P2 |
| U-02 | 单义主区 | 默认单栏对话流；会话列表为可折叠抽屉 | P2 |
| U-03 | 轨迹内联 | 工具调用/中间产物折叠成时间线，非独立 tab 页 | P2 |
| U-04 | 状态可见 | 顶栏/托盘/窗口标题常驻 agent 状态（运行/待批/后台任务） | P2 |
| U-05 | 原生交互 | 托盘、全局热键 Spotlight 输入、通知点击定位、拖放附件 | P2 |
| U-06 | 视觉自研 | 自有设计 token（字体/密度/圆角/配色），**不使用官方 `--dsw-alias-*`** | P2 |
| U-07 | 密度可调 | 紧凑/舒适两档；长会话自动分段可折叠 | P2 |
| U-08 | 多窗口 | 会话可弹出独立窗口，带独立状态栏 | P2 |

## 3. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 启动速度 | 冷启动到可用 UI ≤ 3s（中端 Win 机器）；宿主热启动（常驻）≤ 300ms 唤起 |
| 内存 | 常驻态 ≤ 400MB（Chromium 多进程开销之上尽量收敛）；空闲会话不活跃窗口自动冻结 |
| 安全 | 默认零端口；renderer 全程 `contextIsolation: true` + 沙箱 + 白名单 IPC（见 08） |
| 隐私 | 使用统计本地化默认关闭；外发仅限官方模型 API 与用户启用的 web 能力 |
| 兼容性 | Win10/11（x64, arm64 可选）优先；macOS/Linux 后置（M5 起） |
| 可审查性 | 每次桌面动作产生结构化日志；敏感操作（剪贴板写、文件删除、外链）过 approval 服务 |
| 版本纪律 | 钉死上游 rc 基线；升级可回滚；文档级 breaking-change 迁移记录（ADR-005） |

## 4. 非目标（明确不做，防范围蔓延）

- **不自研主面 UI**：M1–M3 主面始终为官方 UI 复用形态；自绘 Desktop UI 列为 P2 技术债（ADR-006 启用前不投入）。
- 不做多用户/云同步；不引入账号体系（与官方一致仅本机）。
- 不把桌面做成「插件商店」运营平台；插件装载仍走 `dsh plugin` / patch 机制。
- 不承诺修复上游内核缺陷（如 Windows 子进程闪窗根因在 `dsh-subprocess-local`，见 dsh-terminal 插件边界说明）。

## 5. 成功标准（3 个月可验证）

1. 同一台机器：桌面与 `dsh web` 共享数据，桌面默认零端口。
2. **主面为官方 UI 复用**：M1 起即能在桌面内完成日常对话全流程（托盘/热键/通知齐全），不依赖浏览器与外部 HTTP 端口。
3. 旧插件不丢弃：`dsh-rule-manager`、`dsh-terminal`、`dsh-restart` 的 host 半零改动可用；client 半随官方 UI 主面原样可用。
4. 桌面能力本身可被 `dsh plugin` 列表看到、可被 patch 增删——验证「桌面=插件树」主张。
5. 托盘 + 全局热键 + 通知成为肌肉记忆（R-05~R-08 全部达成）。